import { createTheme } from "@mui/material";

const theme = createTheme({
	palette: {
		primary: {
			// main: "#0288d1",
			main: "#e91e63",
			
			contrastText: "#fff",
		},
	},
});

export default theme;
